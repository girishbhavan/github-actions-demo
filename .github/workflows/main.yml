name: CI pipeline
on: 
  push:
    branches:
      -main
jobs:
  build:
    runs-on: ubuntu-latest
  steps:
    -uses: action/checkout@v4

    -name: print message
     run: echo "hello github actions"

     -name: check git version
      run: git --version
